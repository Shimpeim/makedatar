% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fill_demographics.R
\name{md_fill_demographics}
\alias{md_fill_demographics}
\title{Propagate per-patient constant fields and fill fallback date columns}
\usage{
md_fill_demographics(
  data,
  vars = c("birth_date", "gender", "height", "weight"),
  id_var = "study_id",
  fill_from = c(visit_date = "ic_date")
)
}
\arguments{
\item{data}{A \code{data.frame} (returned by \code{\link[=md_import_data]{md_import_data()}}).}

\item{vars}{Character vector of column names to broadcast from non-\code{NA}
rows to all rows sharing the same patient ID.  Defaults to the four
demographic constants in the original script:
\code{c("birth_date", "gender", "height", "weight")}.
Columns absent from \code{data} are silently skipped.}

\item{id_var}{Name of the patient identifier column (default \code{"study_id"}).}

\item{fill_from}{Named character vector where each \strong{name} is a target
column and each \strong{value} is the fallback source column.  When a target
cell is \code{NA}, the corresponding source cell is used instead.
Default \code{c(visit_date = "ic_date")} replicates the original script's
registration-visit date fill.  Set to \code{NULL} to disable.
Pairs where either column is absent from \code{data} are silently skipped.}
}
\value{
The modified \code{data.frame} (same dimensions as \code{data}; no new columns
are added).
}
\description{
Propagate per-patient constant fields and fill fallback date columns
}
\details{
\strong{Broadcasting logic}  --  for each variable \code{v} in \code{vars}:
\enumerate{
\item Find rows where \code{v} is non-\code{NA}; keep the first non-\code{NA} value per patient.
\item Use \code{match()} to map every row's patient ID to that reference value.
\item Replace the entire \code{v} column with the looked-up values (patients with no
non-\code{NA} reference row remain \code{NA}).
}

This is functionally equivalent to the original \code{merge()} approach but does
not create \code{.x}/\code{.y} suffix columns.
}
\examples{
# Default usage  --  broadcasts birth_date, gender, height, weight and fills
# visit_date from ic_date:
\dontrun{
data <- md_fill_demographics(data)
}

# Custom: broadcast only one var, no date fill:
\dontrun{
data <- md_fill_demographics(data, vars = "birth_date", fill_from = NULL)
}

}
