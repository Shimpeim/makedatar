% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_col_info.R
\name{md_read_col_info}
\alias{md_read_col_info}
\title{Read and validate the col_info schema sheet}
\usage{
md_read_col_info(path, sheet = "colinfo", na = "NA")
}
\arguments{
\item{path}{Path to the Excel workbook.}

\item{sheet}{Name or index of the col_info sheet (default \code{"colinfo"}).}

\item{na}{String(s) that should be read as \code{NA} (default \code{"NA"}).}
}
\value{
A \code{data.frame} with one row per variable.  Rows are ordered by
\code{varseq}.  Optional annotation columns that were absent from the sheet are
added as \code{NA_character_} columns.
}
\description{
Read and validate the col_info schema sheet
}
\details{
The following columns are \strong{required} and cause an error if absent:
\code{col_name}, \code{col_type}, \code{varseq}, \code{orig_name}.

The following columns are \strong{optional}; they are created as all-\code{NA} columns
if missing from the sheet:
\code{col_label}, \code{ID}, \code{exposure}, \code{background}, \code{outcome}, \code{Psmodel},
\code{transform}, \code{cutoff}, \code{var.score}, \code{var.level}, \code{var.label},
\code{strata_in}, \code{timepoint}.
}
\examples{
\dontrun{
col_info <- md_read_col_info("path/to/workbook.xlsx")
}

}
