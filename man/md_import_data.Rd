% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import_data.R
\name{md_import_data}
\alias{md_import_data}
\title{Import the data sheet using a col_info schema}
\usage{
md_import_data(path, col_info, sheet = "data", skip = 1L, na = "*")
}
\arguments{
\item{path}{Path to the Excel workbook.}

\item{col_info}{A \code{data.frame} returned by md_read_col_info().}

\item{sheet}{Sheet name or index for the data sheet (default \code{"data"}).}

\item{skip}{Number of rows to skip before reading (default \code{1}).
The first skipped row is typically the original EDC column-name row
exported before the renamed headers.}

\item{na}{String(s) to interpret as \code{NA} (default \code{"*"}).}
}
\value{
A \code{data.frame} with columns named according to \code{col_info$col_name},
restricted to rows where \code{col_info$orig_name} is non-\code{NA} and
\code{col_info$col_type} is not \code{"skip"}, ordered by \code{col_info$varseq}.
}
\description{
Import the data sheet using a col_info schema
}
\details{
\code{col_type == "skip"} rows are recognised by \code{readxl::read_excel()} and are
never materialised in the result.  The column ordering step replicates the
\code{order(varseq)} select from the original script, ensuring the output column
sequence always matches the schema regardless of how readxl orders them
internally.
}
\examples{
\dontrun{
col_info <- md_read_col_info("workbook.xlsx")
data     <- md_import_data("workbook.xlsx", col_info)
}

}
