% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trans_functions.R
\name{mf.value_at_spec_visit}
\alias{mf.value_at_spec_visit}
\title{Look up the value of a variable at a specific visit}
\usage{
mf.value_at_spec_visit(
  data,
  var.targ,
  var.visit,
  visit_name,
  var.key,
  ...,
  id_cols = character(0)
)
}
\arguments{
\item{data}{data.frame}

\item{var.targ}{column name -- the target variable to look up}

\item{var.visit}{column name -- the visit-label column}

\item{visit_name}{character(1) -- the visit label to filter on}

\item{var.key}{column name -- the join key (e.g. subject ID)}

\item{...}{ignored}

\item{id_cols}{character vector of ID column names}
}
\value{
vector of looked-up values, same length as \code{nrow(data)}
}
\description{
For each row, returns the value of \code{var.targ} recorded at the visit
whose label equals \code{visit_name}, matched via the key column
\code{var.key}.  Duplicate keys at the target visit are resolved by
keeping the first occurrence.
}
