% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_var_roles.R
\name{md_extract_var_roles}
\alias{md_extract_var_roles}
\title{Extract typed variable-role sub-tables from a col_info data.frame}
\usage{
md_extract_var_roles(col_info)
}
\arguments{
\item{col_info}{A \code{data.frame} returned by \code{\link[=md_read_col_info]{md_read_col_info()}}.}
}
\value{
A named list of data.frames, one per variable role.  All elements
are always present; an empty data.frame (0 rows) is returned when no
variables satisfy a role's filter.  See \strong{Details} for the mapping to the
original \verb{var.*} objects.
}
\description{
Extract typed variable-role sub-tables from a col_info data.frame
}
\details{
\tabular{lll}{
   List element \tab Original object \tab Filter \cr
   \verb{$id} \tab \code{var.ID} \tab \code{ID} non-NA \cr
   \verb{$exposure} \tab \code{var.exposure} \tab \code{exposure} non-NA, \code{col_type == "text"} \cr
   \verb{$exposure_conti} \tab \code{var.exposure.conti} \tab \code{exposure} non-NA, \code{col_type == "numeric"} \cr
   \verb{$background} \tab \code{var.background} \tab \code{background} non-NA, \code{col_type == "text"} \cr
   \verb{$background_conti} \tab \code{var.background.conti} \tab \code{background} non-NA, \code{col_type == "numeric"} \cr
   \verb{$event} \tab \code{var.event} \tab \code{outcome \%in\% c("event", "rank")} \cr
   \verb{$time_to_event} \tab \code{var.timetoevent} \tab \code{outcome == "time"} \cr
   \verb{$censored_time} \tab \code{var.cens_timetoevent} \tab \code{outcome == "censored.time"} \cr
   \verb{$psmodel} \tab \code{var.Psmodel} \tab \code{Psmodel == "1"} \cr
   \verb{$transform} \tab \code{var.trans} \tab \code{transform} non-NA \strong{and} \code{cutoff} NA \cr
   \verb{$cutoff} \tab \code{var.cutoff} \tab \code{cutoff} non-NA \cr
   \verb{$label} \tab \code{var.label} \tab \code{var.label} non-NA \cr
   \verb{$label_orig} \tab \code{var.label.orig_data} \tab subset of \verb{$label}: \code{orig_name} non-NA, \code{col_type != "skip"} \cr
   \verb{$label_derived} \tab \code{var.label.transformed_data} \tab subset of \verb{$label}: \code{orig_name} NA, \code{col_type != "skip"} \cr
   \verb{$strata_in} \tab \code{var.strata_in} \tab \code{strata_in} non-NA \cr
}
}
\examples{
\dontrun{
col_info  <- md_read_col_info("workbook.xlsx")
var_roles <- md_extract_var_roles(col_info)
var_roles$id          # ID column names
var_roles$exposure    # categorical exposure variables
var_roles$transform   # variables requiring transformation
}

}
