% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_dsl.R
\name{.parse_transform_dsl}
\alias{.parse_transform_dsl}
\title{Parse a transform DSL string into a function name and argument list}
\usage{
.parse_transform_dsl(expr)
}
\arguments{
\item{expr}{A single character string from the \code{transform} column of a
col_info table.  \code{NA} is allowed and returns \code{list(func_name = "identity", args = list())}.}
}
\value{
A named list with two elements:
\itemize{
\item \code{func_name}  --  character(1), the function to call.
\item \code{args}       --  list of character strings, one per argument.
}
}
\description{
The transform DSL encodes a function call as a plain string so it can be
stored in an Excel cell.  The canonical grammar is:
}
\details{
\if{html}{\out{<div class="sourceCode">}}\preformatted{<func_name>_func.(<arg1>,<arg2>,...)
}\if{html}{\out{</div>}}

If the string does not contain the \verb{_func.(} marker the whole string is
treated as a bare function name with no arguments (the common case is
\code{"identity"}).

Arguments are split on the first-level commas only; whitespace around each
argument is stripped.
}
\examples{
\dontrun{
.parse_transform_dsl("mf.bmi_func.(height,weight)")
# list(func_name = "mf.bmi", args = list("height", "weight"))

.parse_transform_dsl("identity")
# list(func_name = "identity", args = list())

.parse_transform_dsl(NA_character_)
# list(func_name = "identity", args = list())
}

}
\keyword{internal}
